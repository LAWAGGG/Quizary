DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
DB_NAME=fastapi_quizary
SECRET_KEY=

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
GEMINI_FALLBACK_MODEL=gemini-3.5-flash-lite

# Domain frontend prod eksak, koma-pisah (tanpa wildcard).
# Ngrok (*.ngrok-free.dev / *.ngrok.io / *.ngrok.app), trycloudflare, loca.lt,
# localhost & LAN sudah diizinkan otomatis — tidak perlu diisi untuk dev/tunnel.
# Contoh: CORS_EXTRA_ORIGINS=https://quizary.id,https://app.quizary.id
CORS_EXTRA_ORIGINS=
